@model sourceBackup.Portal.Models.SupportRequest

@{
    ViewData["Title"] = "Support";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Support</h1>

<h6>Here you can send a message to the developer, letting them know what you need to know, in order to get the most out of the product.  
    <br/>
    Enter your details below.
    </h6>
<hr />
@if (Model.SendSuccess)
{
    <div>Your support request has been sucessfully sent!</div>
}
else
{
    <div class="row">
        <div class="col-md-4 col-lg-12">
            <form asp-action="Support">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group col-4">
                    <label asp-for="Email" class="control-label"></label>
                    <input asp-for="Email" class="form-control" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="form-group form-check">
                    <label class="form-check-label">
                        <input class="form-check-input" asp-for="CarbonCopyUser" /> @Html.DisplayNameFor(model => model.CarbonCopyUser)
                    </label>
                </div>
                <div class="form-group text-area">
                    <label asp-for="RequestBody" class="control-label"></label>
                    <textarea asp-for="RequestBody" class="form-control text-area"></textarea>
                    <span asp-validation-for="RequestBody" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Send Request" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
}


<div>
    <a asp-action="Index">Back to Home</a>
</div>

